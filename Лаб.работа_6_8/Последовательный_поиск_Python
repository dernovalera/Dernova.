#Последовательный (линейный) поиск, исходный код был написан на C++, ниже представлена его реализация на Python
def linear_search(arr, target):  # объявление функции, которая принимает список и искомое значение
    for i, value in enumerate(arr):  # перебор элементов списка с индексом (i) и значением (value)
        if value == target:  # если текущий элемент равен искомому
            return i  # возвращаем индекс найденного элемента
    return -1  # если элемент не найден ни один раз, возвращаем -1 (значит он отсутствует)

# основной код
array = [3, 5, 2, 7, 9, 1, 4]  # создаем список
target = 7  # искомое значение

result = linear_search(array, target)  # вызов функции поиска и сохранение результата

if result != -1:  # если результат не равен -1, значит элемент найден
    print(f"Элемент найден на позиции: {result}")  # выводим позицию, где найден элемент
else:  # если элемент не найден
    print("Элемент не найден")  # выводим сообщение, что элемент не найден
