#Сортировка обменом (пузырьком) (Bubble Sort), исходный код был написан на C++, ниже представлена его реализация на Python
def bubble_sort(arr):  # вызывается сортировка пузырьком на Python
    n = len(arr)  # длина массива

    for i in range(n - 1):  # проходим по всем элементам массива
        swapped = False  # если за текущий проход элементы не поменены местами, то значит массив отсортирован и цикл можно заканчивать

        for j in range(n - i - 1):  # элементы, находящиеся в конце уже отсортированы, поэтому смотрим дальше
            if arr[j] > arr[j + 1]:  # сравниваем соседние элементы, если левый больше
                arr[j], arr[j + 1] = arr[j + 1], arr[j]  # меняем местами
                swapped = True  # обмен произведен, значит массив езе не отсортирован
        if not swapped:  # если не было обменов, массив отсортирован
            break
    return arr  # возвращаем отсортированный массив

test_array = [64, 34, 25, 12, 22, 11, 90]   # начальный массив
print("Исходный массив:", test_array)  # выводим исходный массив

bubble_sort(test_array)  # вызываем сортировку

print("Отсортированный массив:", test_array)  # выводим отсортированный массив
