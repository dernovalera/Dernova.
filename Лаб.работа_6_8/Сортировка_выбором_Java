//Сортировка выбором (Selection Sort), исходный код был написан на Python, ниже представлена его реализация на Java
public class Main {
    public static void selectionSort(int[] arr) {  // принимает массив целых чисел для сортировки
        int n = arr.length;  // создание переменной, равной длине массива, чтобы использовать её для контроля циклов

        for (int i = 0; i < n - 1; i++) {  // цикл, который пробегает по каждому элементу массива, кроме последнего
            int minIndex = i;  // индекс минимального элемента в данном цикле

            for (int j = i + 1; j < n; j++) {  // цикл ищет минимальный элемент в оставшейся части массива
                if (arr[j] < arr[minIndex]) {  // если найден элемент меньше прошлого минимального
                    minIndex = j;  // обновляем минимальный элемент
                }
            }
            // обмен элементов
            int temp = arr[minIndex];  // переменная, которой присваивается значение элемента массива (сохраняем значение для перемещения на другую позицию)
            arr[minIndex] = arr[i];  // заменяет значение элемента массива, который находится на позиции minIndex, на значение элемента, находящегося на позиции i
            arr[i] = temp;  // значение, которое было сохранено в переменной temp, присваивается элементу по индексу i
        }
    }

    public static void printArray(int[] arr) {  // объявляем метод, который выводит содержимое на экран
        for (int value : arr) {  // перебираются все элементы массива
            System.out.print(value + " "); // каждый элемент выводится на экран с пробелом
        }
        System.out.println();  // перенос на другую строчку (для красоты)
    }

    public static void main(String[] args) {  // вход в программу
        int[] array = {64, 25, 12, 22, 11};  // создается массив

        System.out.println("Исходный массив:");  // выводится данная строчка
        printArray(array);  // выводится начальный массив

        selectionSort(array);  // вызывается сортировка массива

        System.out.println("Отсортированный массив:");  // выводится данная строчка
        printArray(array);  // выводится отсортированный массив
    }
}
